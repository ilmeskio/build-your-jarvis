# Local n8n state persisted by docker compose
data

# We keep Node dependencies and Slidev build output out of git so teammates can install/build locally without churn.
node_modules
.slidev
dist

# We keep a committed ".env" for workshop defaults, but we still ignore personal overrides.
# This lets everyone start from the same baseline while keeping local tweaks out of git.
.env.*
